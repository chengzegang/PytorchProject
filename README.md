# A simple Pytorch Template for Deep Learning with DDP support
This template trains an autoencoder ViT model on all images in given image folder.

```bash
python -m src --dataset-dir /path/to/dataset
```